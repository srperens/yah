{
  "name": "yah",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "You Are Here — an interactive decode of the Pioneer/Voyager pulsar map.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
